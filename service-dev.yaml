apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: my-service-dev   # ✅ Dev Cloud Run service
spec:
  template:
    spec:
      containers:
        - image: my-app  # ✅ Placeholder (mapped in Cloud Build)
